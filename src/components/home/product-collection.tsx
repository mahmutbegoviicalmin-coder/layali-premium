"use client";

/**
 * Legacy carousel — kept for reference. Active homepage collection is
 * `ProductCollection.tsx` (uses products context / API).
 */
export function ProductCollectionLegacy() {
  return null;
}
